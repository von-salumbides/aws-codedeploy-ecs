version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: {{ td_details.taskDefinitionArns[0] }}
        LoadBalancerInfo:
          ContainerName: "devops-api-poc"
          ContainerPort: 80
        CapacityProviderStrategy:
          - Base: {{ desired_count }}
            CapacityProvider: "FARGATE"
            Weight: {{ desired_count }}
